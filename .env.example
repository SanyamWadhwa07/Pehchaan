# Supabase (mobile app — anon key only; safe to bundle via react-native-config)
# Dashboard → Project Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key

# Service role: use Supabase Dashboard / CLI secrets for edge functions — NOT in this file (never ship in the app).

# Site package v2 decrypt — base64 of 32 raw bytes; must match Edge secret SITE_PACKAGE_MASTER_KEY (hackathon dev only; do not ship production site master in field APKs).
SITE_PACKAGE_MASTER_KEY=

# Attendance local purge policy (WatermelonDB tombstone after successful server mirror).
# false / unset = move to `purged` as soon as server reports `verified` (default hackathon).
# true = stay `verified` until integration_push_status is `pushed` or `not_applicable`, then `purged`.
ATTENDANCE_PURGE_AFTER_INTEGRATION=

# Integration layer — leave empty until NHAI DataLink 3.0 credentials are provided
INTEGRATION_API_KEY=
INTEGRATION_ENDPOINT=

